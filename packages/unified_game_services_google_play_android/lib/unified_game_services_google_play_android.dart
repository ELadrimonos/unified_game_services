/// Native Google Play Games provider for `unified_game_services`, backed by the
/// Play Games v2 Java SDK over `package:jni`. Android only; fires native
/// achievement/leaderboard UI.
///
/// See [GooglePlayAndroidProvider]. The generated JNI bindings live in
/// `src/playgames_bindings.dart` (regenerate with
/// `tool/regenerate_bindings.sh`).
library;

export 'src/google_play_android_provider.dart';

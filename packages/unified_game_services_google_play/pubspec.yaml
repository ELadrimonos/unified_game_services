name: unified_game_services_google_play
resolution: workspace
description: Google Play Games provider for unified_game_services. Deferred — not yet implemented.
version: 0.0.1
repository: https://github.com/ELadrimonos/unified_game_services

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

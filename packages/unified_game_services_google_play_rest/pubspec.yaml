name: unified_game_services_google_play_rest
resolution: workspace
description: Google Play Games provider for unified_game_services over the REST Games API v1 (pure Dart, no Flutter). Achievements + leaderboards.
version: 0.0.1
repository: https://github.com/ELadrimonos/unified_game_services

environment:
  sdk: ^3.12.0

dependencies:
  crypto: ^3.0.6
  http: ^1.2.0
  unified_game_services_platform_interface: ^0.0.1

dev_dependencies:
  args: ^2.6.0
  lints: ^6.0.0
  test: ^1.25.6

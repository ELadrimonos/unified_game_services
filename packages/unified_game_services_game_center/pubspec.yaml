name: unified_game_services_game_center
resolution: workspace
description: Apple Game Center provider for unified_game_services. Deferred — not yet implemented.
version: 1.0.0
repository: https://github.com/ELadrimonos/unified_game_services

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

name: unified_game_services_playfab
resolution: workspace
description: PlayFab provider for unified_game_services, using the PlayFab Client API over REST (pure Dart).
version: 0.0.1
repository: https://github.com/ELadrimonos/unified_game_services

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  http: ^1.2.0
  unified_game_services_platform_interface: ^0.0.1

dev_dependencies:
  args: ^2.6.0
  lints: ^6.0.0
  test: ^1.25.6

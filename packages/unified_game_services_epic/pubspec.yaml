name: unified_game_services_epic
resolution: workspace
description: Epic Online Services (EOS) provider for unified_game_services. Planned — not yet implemented.
version: 0.0.1
repository: https://github.com/ELadrimonos/unified_game_services

topics:
  - game-services
  - achievements
  - leaderboards
  - epic-games
  - gamedev

# Desktop for now (EOS via REST/C SDK targets desktop first); revisit when
# the provider is implemented.
platforms:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

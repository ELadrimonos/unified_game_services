# Dart / pub — https://dart.dev/tools/private-files
.dart_tool/
.packages
.pub/
.pub-cache/
build/
# Flutter plugin resolution cache (regenerated by `flutter pub get`)
.flutter-plugins-dependencies

# Test & coverage output
coverage/
.test_coverage.dart
test/.test_coverage.dart

# Generated API docs
doc/api/

# Logs & OS cruft
*.log
.DS_Store

# Local credentials — keep the templates, never the filled-in files
.env
.env.local
*.env
!.env.template

# Steamworks dev tooling output (SDK extract, generated bindings, native libs)
.steamworks/
steam_appid.txt
*.dll
*.dylib
*.so

# Generated dart_mappable code is committed on purpose (consumers should not
# need build_runner). Do NOT ignore *.mapper.dart.
